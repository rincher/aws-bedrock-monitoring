apiVersion: 1

contactPoints:
  - orgId: 1
    name: Bedrock-AI-Webhook
    receivers:
      - uid: bedrock-webhook
        type: webhook
        settings:
          url: https://__API_GATEWAY_HOST__/prod/webhook
          httpMethod: POST
